using System.Runtime.InteropServices;
using UnityEngine;

public class sdf : MonoBehaviour
{

        void Update()
        {
            if (Input.GetKey("escape"))
                Application.Quit();

        }
}
